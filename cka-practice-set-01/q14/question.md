# Q14 — kubelet Troubleshooting (8점)

환경 구성 스크립트가 가능하면 Worker Node의 kubelet을 중지한다.

문제가 구성되었다면:
1. 문제가 발생한 Node를 식별
2. kubelet 상태와 journal 확인
3. kubelet 정상화
4. Node를 `Ready`로 복구

`set_question.sh`가 SKIP을 출력하면 해당 Killercoda 환경에서는 건너뛴다.
